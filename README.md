# Bot Battle of Talimgchan - Discord Stock Checker

Discord Bot สำหรับเช็คสต็อกสินค้าและแจ้งเตือนอัตโนมัติ + Web Dashboard สำหรับจัดการรายการสินค้า

## ✨ Features

- ✅ เช็คสต็อกสินค้าอัตโนมัติจากหลาย URL
- ✅ Web Dashboard สำหรับเพิ่ม/ลบ/จัดการสินค้า
- ✅ แจ้งเตือนเมื่อสินค้ามีของเข้า
- ✅ เปิด/ปิดการเช็คแต่ละสินค้าได้
- ✅ รองรับการ deploy บน Railway

## 🚀 Deploy บน Railway

### ขั้นตอนการ Deploy

1. **Push โค้ดขึ้น GitHub**
   ```bash
   git add .
   git commit -m "Add web dashboard"
   git push origin main
   ```

2. **สร้างโปรเจค Railway**
   - ไปที่ https://railway.app/
   - คลิก "Start a New Project"
   - เลือก "Deploy from GitHub repo"
   - เชื่อมต่อ GitHub repository ของคุณ

3. **ตั้งค่า Environment Variables**
   
   ใน Railway Dashboard ให้เพิ่ม Environment Variables:
   
   ```
   DISCORD_TOKEN=your_discord_bot_token
   CHANNEL_ID=your_channel_id
   PORT=5000
   ALERT_COUNT=10
   ALERT_INTERVAL=10
   CHECK_INTERVAL_MIN=30
   CHECK_INTERVAL_MAX=60
   ```

4. **เพิ่ม Volume สำหรับเก็บข้อมูล** (ถ้าต้องการ)
   - ไปที่ Service → Volumes
   - Mount Path: `/app/data`
   - เพื่อเก็บไฟล์ `products.json` ถาวร

5. **Deploy**
   - Railway จะ deploy ทั้ง Web และ Worker อัตโนมัติ
   - Web Dashboard จะรันที่ port ที่กำหนด
   - Bot จะเริ่มเช็คสต็อกทันที

## 🌐 การใช้งาน Web Dashboard

1. **เข้าถึง Dashboard**
   - URL: `https://your-railway-url.up.railway.app`
   
2. **เพิ่มสินค้า**
   - ใส่ชื่อสินค้า (optional)
   - ใส่ URL สินค้า
   - คลิก "เพิ่มสินค้า"

3. **จัดการสินค้า**
   - **หยุด/เริ่ม** - เปิด/ปิดการเช็คสินค้า
   - **ลบ** - ลบสินค้าออกจากรายการ

## 💰 ค่าใช้จ่าย Railway

- **Free Plan**: $5 credit ฟรีต่อเดือน (พอสำหรับใช้งานเบาๆ)
- **Hobby Plan**: $5/เดือน ($5 credit + unlimited usage)
- **Pro Plan**: $20/เดือน

**สำหรับโปรเจคนี้:**
- Web + Worker = 2 services
- ใช้ทรัพยากรน้อย (น่าจะใช้ $5 credit ฟรีได้)
- ถ้าต้องการรันตลอด 24/7 แนะนำ Hobby Plan ($5/เดือน)

### 📝 Environment Variables ที่จำเป็น

| Variable | คำอธิบาย | จำเป็น |
|----------|---------|--------|
| `DISCORD_TOKEN` | Discord Bot Token | ✅ |
| `CHANNEL_ID` | Discord Channel ID ที่จะส่งข้อความ | ✅ |
| `PRODUCT_URL` | URL สินค้าที่ต้องการเช็ค | ❌ (มี default) |
| `ALERT_COUNT` | จำนวนครั้งที่แจ้งเตือน | ❌ (default: 10) |
| `ALERT_INTERVAL` | ช่วงเวลาระหว่างการแจ้งเตือน (วินาที) | ❌ (default: 10) |
| `CHECK_INTERVAL_MIN` | เวลาเช็คสต็อกขั้นต่ำ (วินาที) | ❌ (default: 30) |
| `CHECK_INTERVAL_MAX` | เวลาเช็คสต็อกสูงสุด (วินาที) | ❌ (default: 60) |

### 🛠️ ติดตั้งและรันในเครื่อง (Local)

1. **ติดตั้ง Dependencies**
   ```bash
   pip install -r requirements.txt
   ```

2. **สร้างไฟล์ `.env`**
   ```bash
   cp .env.example .env
   ```
   
3. **แก้ไขไฟล์ `.env`** ใส่ค่าที่ต้องการ

4. **รันโปรแกรม**
   ```bash
   python main.py
   ```

### 📂 โครงสร้างไฟล์

```
Bot-Battle-ofTalimgchan/
├── main.py              # ไฟล์หลักของ Bot
├── requirements.txt     # Python dependencies
├── Procfile            # สำหรับ Railway deployment
├── runtime.txt         # ระบุ Python version
├── .env.example        # ตัวอย่าง environment variables
└── README.md           # คู่มือการใช้งาน
```

### ⚙️ คุณสมบัติ

- ✅ เช็คสต็อกสินค้าอัตโนมัติ
- ✅ แจ้งเตือนเมื่อสินค้ามีของเข้า
- ✅ ปรับแต่งจำนวนและความถี่การแจ้งเตือน
- ✅ รองรับการ deploy บน Railway
- ✅ ใช้ Environment Variables เพื่อความปลอดภัย

### 🔒 ความปลอดภัย

- ❌ **อย่า commit ไฟล์ `.env`** ที่มี Token จริงลง Git
- ✅ ใช้ Environment Variables บน Railway
- ✅ เก็บ Token ให้ปลอดภัย
